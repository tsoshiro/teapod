<%= form_for(@content, url: input_path) do |f|%>
  <%= f.text_field :url, class: 'form-control' %>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
  <%= f.submit "リセット", type: :reset, class: "btn btn-default clear_form" %>
<% end %>
